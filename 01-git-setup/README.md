# Day 1: Git and GitHub Setup

**Date:** 01/06/2026

**Session Mode:** Online

## Task Description
Set up Git version control, configure user identity, create a GitHub repository, and push the first set of documentation files. This establishes the foundation for tracking all internship work.

## Deliverables Completed
- Installed Git version 2.54.0 on Windows 11
- Configured global `user.name` and `user.email` via PowerShell
- Created a public repository `Internship-IOT-ML` on GitHub
- Cloned the repository locally using `git clone`
- Created folder structure: root `README.md` and `01-git-setup/README.md`
- Wrote initial documentation following the required structure
- Staged, committed, and pushed changes using `git add .`, `git commit`, and `git push -u origin main`

## Key Learnings
- Understood the difference between local (Git) and remote (GitHub) repositories
- Learned to write meaningful commit messages with prefixes (`doc:`, `feat:`)
- Handled line‑ending warnings (LF vs CRLF) on Windows – normal behaviour
- Verified successful upload by checking the repository on GitHub.com
- Mastered basic Git commands: `git clone`, `git add`, `git commit`, `git push`

## Next Steps
- Set up SSH key for smoother authentication (optional)
- Prepare for Day 2: Firebase Hosting and portfolio deployment

## Resources
- Git: https://git-scm.com/
- GitHub: https://github.com/
- Git documentation: https://git-scm.com/doc